sudo: false
language: csharp
solution: Broadcast.sln
 
install:
  - sudo apt-get install nunit-console
  - nuget restore Broadcast.sln
 
script:
  - xbuild /p:Configuration=Release Broadcast.sln
  - nunit-console ./src/Tests/Broadcast.Test/bin/Release/Broadcast.Test.dll